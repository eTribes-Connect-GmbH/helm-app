name: Tests

on:
  pull_request:
  push:

jobs:
  run-tests:
    permissions:
      contents: write
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: azure/setup-helm@v4
      - name: Install helm unittest plugin
        run: helm plugin install https://github.com/helm-unittest/helm-unittest
      - name: Run helm unittest
        run: helm unittest charts/app
      - name: Set up chart-testing
        uses: helm/chart-testing-action@v2
      - name: Lint charts
        run: |
          ct lint --charts charts/app \
            --helm-extra-args "--strict" \
            --helm-extra-args '--values charts/app/values-ci.yaml'
      # - name: Install KinD
      #   uses: helm/kind-action@v1
      # - name: Run ct install
      #   run: ct install --charts charts/app
      # - name: Validate
      #   run: kubectl wait --for=condition=ready pod -l app.kubernetes.io/instance=app --timeout=120s
